#include "holberton.h"
/**
 * _puts - a function that prints a string, followed by a new line.
 * @str: the string to print.
 */
void _puts(char *str)
{
int i;
for (i = 0; s[i]; i++)
{
_putchar (s[i]);
}
_putchar ('\n');
}

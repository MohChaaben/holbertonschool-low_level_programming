#include "holberton.h"
/**
 * mul - a function that multiplies two integers.
 * @a: the 1st integer of the multiplication.
 * @b: the 2nd integer of the multiplication.
 * Return: r multiplication result.
 */
int mul(int a, int b)
{
int r;
r = a * b;
return (r);
}
